{
  "fault_id": "rust-badvalidation-07",
  "fault_type": "incorrect API behavior",
  "language_runtime": "rust",
  "fixture_app": "fixture-app-rust",
  "base_ref": "fault/rust-badvalidation-07-base-v2",
  "service_name": "fixture-app-rust",
  "error_summary": "incorrect API behavior: /format-order accepts and echoes back negative amounts instead of rejecting them with 400",
  "expected_behavior": "GET /format-order?amount=-5 returns HTTP 400 instead of 200, while GET /format-order?amount=5 still returns HTTP 200 with {\"amount\":5,\"currency\":\"USD\"}."
}
