{
  "fault_id": "rust-config-04",
  "fault_type": "configuration failure",
  "language_runtime": "rust",
  "fixture_app": "fixture-app-rust",
  "base_ref": "fault/rust-config-04-base",
  "service_name": "fixture-app-rust",
  "error_summary": "configuration failure: an invalid MAX_ORDER_AMOUNT value silently disables ordering instead of falling back to the documented default",
  "expected_behavior": "With MAX_ORDER_AMOUNT set to a non-numeric value, GET /validate-order?amount=50000 still returns 'accepted' (falling back to the documented 100000 default), while a genuinely over-limit amount is still rejected under the default."
}
