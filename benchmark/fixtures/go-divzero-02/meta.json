{
  "fault_id": "go-divzero-02",
  "fault_type": "runtime exception",
  "language_runtime": "go",
  "fixture_app": "fixture-app-go",
  "base_ref": "fault/go-divzero-02-base",
  "service_name": "fixture-app-go",
  "error_summary": "panic: runtime error: integer divide by zero in handleDivideShare (divide.go)",
  "expected_behavior": "GET /divide-share?total=X&parts=0 returns HTTP 400 instead of panicking, GET /divide-share?total=100&parts=4 still returns 25, and the process keeps handling requests normally afterward."
}
